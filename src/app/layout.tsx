import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Collectiva - Make Solidarity Simple",
  description: "Create your portfolio of charitable organizations and make a lasting impact through monthly subscriptions. Our AI ensures your donations reach the most efficient organizations.",
  keywords: "charity, donations, NGO, solidarity, fundraising, AI, portfolio, monthly subscriptions",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
